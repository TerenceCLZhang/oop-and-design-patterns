package OOP_Principles.Composition;

public class Composition {

  /*
   * Composition involves creating complesx objects by combing simpler objects or components.
   * In composition, objects are assembled together to form larger structures, with each component
   * object maintaining its own state and behaviour. Composition is often described in terms of a
   * "has-a" relationship, where one object "has" another object as a part of its state.
   */

  public void Example() {
    Car car = new Car();
    car.StartCar();
  }
}
